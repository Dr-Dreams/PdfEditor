{
  "name": "pdfeditor",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-everything": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT License",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
